# Django + DRF
Django>=5.1,<6
djangorestframework>=3.15
django-cors-headers>=4.6
django-filter>=24.3

# Realtime (Socket.IO protocol — frontend socket.io-client bilan mos)
python-socketio[client]>=5.11
python-engineio>=4.9

# ASGI server (Socket.IO WebSocket; Windows/Linux)
uvicorn[standard]>=0.32

# WSGI (faqat config.wsgi_socket — legacy / maxsus test)
waitress>=3.0

# Production DB (ixtiyoriy; standart SQLite)
# psycopg[binary]>=3.2
